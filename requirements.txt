# Runtime dependencies only (range constraints; exact versions pinned by uv.lock).
# Build/dev tools live in pyproject [dependency-groups].
markdown>=3.5,<4
pygments>=2.17,<3
networkx>=3.0,<4
pillow>=10.0,<13
